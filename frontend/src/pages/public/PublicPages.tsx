import { Link } from 'react-router-dom'
import { albums, announcements, demoImages, events, ministries, pastEvents, sermons } from '../../data/siteContent'
import { AnnouncementCard, Cta, EventCard, GalleryLightbox, MinistryCard, PageHeader, SermonCard } from '../../components/public/PublicElements'
import { Reveal } from '../../components/animation/Reveal'

export function AboutPage() { return <><PageHeader eyebrow="About the parish" title="A home being prepared with care." intro="This page uses clearly marked demo content, ready to be replaced by the parish's confirmed story and history." image={demoImages.sanctuary} /><section className="section"><div className="container prose-grid"><Reveal><p className="eyebrow">Who we are</p><h2 className="heading">A parish story will live here.</h2></Reveal><Reveal delay={.1}><p className="lede">The future About page can hold the real history, spiritual heritage, leadership, and community life of St. Anthony's Malankara Catholic Church. Until that information is supplied, this is intentional placeholder copy—not a claim about the parish.</p><blockquote>“A space for real stories, shared faithfully when they are ready.”</blockquote></Reveal></div></section><section className="section--tight"><div className="container split-image"><img src={demoImages.gathering.src} alt={demoImages.gathering.alt} /><div><p className="eyebrow">Faith & community</p><h2 className="heading heading--small">Made for reflection, welcome, and belonging.</h2><p>Rich text, photographs, quotations, and a future parish timeline can be arranged here without changing the page structure.</p></div></div></section><Cta title="Come and discover parish life." /></> }
export function MinistriesPage() { return <><PageHeader eyebrow="Parish life" title="Many ways to grow together." intro="These ministry entries are fictional placeholders, designed for the real ministries the parish will later share." image={demoImages.hands} /><section className="section"><div className="container card-grid">{ministries.map((item, index) => <Reveal key={item.id} delay={index * .08}><MinistryCard ministry={item} /></Reveal>)}</div></section><Cta title="Find a way to connect." to="/contact" label="Get in touch" /></> }
export function EventsPage() { return <><PageHeader eyebrow="Events" title="Gatherings to look forward to." intro="Future parish events will appear here. The entries below are fictional demo content only." image={demoImages.gathering} /><section className="section"><div className="container"><div className="section-head"><div><p className="eyebrow">Calendar</p><h2 className="heading">Upcoming events</h2></div></div><div className="card-grid">{events.map((item, index) => <Reveal key={item.id} delay={index * .08}><EventCard event={item} /></Reveal>)}</div></div></section><section className="section section--muted"><div className="container"><p className="eyebrow">Archive</p><h2 className="heading heading--small">Past events</h2><div className="card-grid card-grid--single">{pastEvents.map((item) => <EventCard key={item.id} event={item} />)}</div></div></section></> }
export function AnnouncementsPage() { const [featured, ...rest] = announcements; return <><PageHeader eyebrow="Parish news" title="Notices, shared with care." intro="All announcements shown here are fictional demo content. Sensitive future notices will be managed appropriately." /><section className="section"><div className="container"><article className="featured-note"><p className="eyebrow">Featured · {featured.category}</p><h2 className="heading">{featured.title}</h2><p>{featured.summary}</p></article><div className="announcement-grid">{rest.map((item, index) => <Reveal key={item.id} delay={index * .08}><AnnouncementCard item={item} /></Reveal>)}</div></div></section></> }
export function SermonsPage() { return <><PageHeader eyebrow="Messages" title="Reflections for the journey." intro="This archive is populated with demo messages only. Future entries can link to hosted video without storing video files here." image={demoImages.prayer} /><section className="section"><div className="container"><article className="featured-sermon"><img src={sermons[0].thumbnail.src} alt={sermons[0].thumbnail.alt} /><div><p className="eyebrow">Featured message</p><h2 className="heading">{sermons[0].title}</h2><p>{sermons[0].description}</p><a className="button button--primary" href="#" onClick={(event) => event.preventDefault()}>Watch when available</a></div></article></div></section><section className="section section--muted"><div className="container"><p className="eyebrow">Archive</p><h2 className="heading heading--small">More reflections</h2><div className="card-grid">{sermons.slice(1).map((item) => <SermonCard key={item.id} sermon={item} />)}</div></div></section></> }
export function GalleryPage() { return <><PageHeader eyebrow="Gallery" title="Moments held close." intro="These images are curated demo photography. Real parish albums can replace them through the same content structure." image={demoImages.architecture} /><section className="section"><div className="container"><div className="album-list">{albums.map((album) => <article key={album.id} className="album"><img src={album.cover.src} alt={album.cover.alt} loading="lazy" /><div><p className="eyebrow">Demo album</p><h2>{album.title}</h2><p>{album.description}</p></div></article>)}</div><GalleryLightbox albums={albums} /></div></section></> }
export function ContactPage() { return <><PageHeader eyebrow="Visit & contact" title="Come as you are." intro="Address, service times, phone, and email are intentionally marked as forthcoming until the parish confirms them." image={demoImages.sanctuary} /><section className="section"><div className="container contact-grid"><div><p className="eyebrow">Visit us</p><h2 className="heading heading--small">Details are forthcoming.</h2><dl className="contact-details"><div><dt>Address</dt><dd>Address to be confirmed</dd></div><div><dt>Service information</dt><dd>Schedule to be confirmed</dd></div><div><dt>Contact</dt><dd>Email and phone to be confirmed</dd></div></dl></div><div className="map-placeholder" role="img" aria-label="Map location placeholder">Map location placeholder</div></div></section><section className="section section--muted"><div className="container form-wrap"><div><p className="eyebrow">Send a note</p><h2 className="heading heading--small">We would love to hear from you.</h2><p>This visual-only form does not send or store information yet.</p></div><form onSubmit={(event) => event.preventDefault()}><label>Name<input required placeholder="Your name" /></label><label>Email<input type="email" required placeholder="you@example.com" /></label><label>Message<textarea required rows={4} placeholder="How can we help?" /></label><button className="button button--primary" type="submit">Send message (demo)</button></form></div></section></> }
export function DonatePage() { return <><PageHeader eyebrow="Give" title="Support what matters." intro="This is a visual demonstration only. No payments, personal financial details, or payment processing are collected in this phase." image={demoImages.hands} /><section className="section"><div className="container donate-grid"><div><p className="eyebrow">Why give</p><h2 className="heading heading--small">A future place for generosity.</h2><p className="lede">The parish will be able to explain real giving opportunities here once categories and payment details are confirmed.</p></div><form className="donate-form" onSubmit={(event) => event.preventDefault()}><p className="eyebrow">Demo interface — non-functional</p><fieldset><legend>Choose an amount</legend><div className="amounts">{['25', '50', '100', '250'].map((amount) => <button type="button" key={amount}>₹{amount}</button>)}</div></fieldset><label>Custom amount<input inputMode="decimal" placeholder="Enter amount" /></label><label>Purpose<select defaultValue="General Fund"><option>General Fund</option><option>Building</option><option>Missions</option><option>Other</option></select></label><button className="button button--primary" type="submit">Continue (demo)</button><p className="quiet">Secure payment details will be added only in a future payment phase.</p></form></div></section></> }
export function NotFoundPage() { return <section className="not-found"><div className="container"><p className="eyebrow">404</p><h1 className="display">This path leads somewhere else.</h1><p className="lede">The page you are looking for is not available.</p><Link className="button button--primary" to="/">Return home</Link></div></section> }
